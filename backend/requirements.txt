# backend/requirements.txt
# Python 3.12  |  CUDA 12.1+
# Dùng >= để tránh conflict với gradio và các package đã có trên server

# Core serving
fastapi>=0.115.2
uvicorn[standard]>=0.30.6
python-multipart>=0.0.18
starlette>=0.40.0

# Deep learning (torch cài riêng với --index-url cu121)
# torch, torchvision — cài ở cell riêng

# Models
transformers>=4.57.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
sentence-transformers>=3.0.0

# Image / data
Pillow>=10.0.0
pandas>=2.0.0
numpy>=1.26.0

# Dataset download
datasets>=2.20.0
huggingface_hub>=0.24.0
